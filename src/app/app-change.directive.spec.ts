import { AppChangeDirective } from './app-change.directive';

describe('AppChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new AppChangeDirective();
    expect(directive).toBeTruthy();
  });
});
